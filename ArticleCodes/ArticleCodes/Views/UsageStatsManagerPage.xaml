<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ArticleCodes.Views.UsageStatsManagerPage">
  <StackLayout Padding="10" Spacing="5" VerticalOptions="StartAndExpand">
    <Button Text="クリア"
            Command="{Binding GetTypeCommand}" />
    <Button Text="取得"
          Command="{ Binding GetPackageCommand }" />
    <Label Text="2016年12月28日から現在までのアプリ実行時間"/>
    <ScrollView>
    <Label Text="{Binding TestLabel}"/>
    </ScrollView>
    
    
    
  </StackLayout>
</ContentPage>